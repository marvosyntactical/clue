model:
  name: "Qwen/Qwen2.5-7B-Instruct"
  quantization: "nf4"
  compute_dtype: "bfloat16"

lora:
  rank: 8
  alpha: 16
  target_modules: "all-linear"
  dropout: 0.0

training:
  optimizer: "adamw"
  lr: 1.0e-3           # Higher LR to converge faster with small data
  weight_decay: 0.0
  batch_size: 1
  grad_accum: 1        # Every sample = 1 optimizer step
  epochs: 5            # Reduced from 10; higher LR compensates
  max_length: 512
  max_grad_norm: 1.0
  seed: 42

clue:
  method: "slao"
  a_init_method: "qr"
  fisher_lambda: 0.1
  fisher_gamma: 0.9
  fisher_samples: 128
  fisher_merge_beta: 0.5

inference:
  max_new_tokens: 512
  temperature: 0.7
  top_p: 0.9
  repetition_penalty: 1.1

ui:
  server_name: "0.0.0.0"
  server_port: 7860
  system_prompt: "You are a helpful assistant."

paths:
  sessions_dir: "clued_in/sessions"
  adapters_dir: "clued_in/adapters"
  current_adapter: "clued_in/adapters/current"
